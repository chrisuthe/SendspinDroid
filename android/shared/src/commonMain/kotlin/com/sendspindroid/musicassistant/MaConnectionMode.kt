package com.sendspindroid.musicassistant

/**
 * Connection modes for the Music Assistant API.
 *
 * Mirrors the app's UserSettings.ConnectionMode for use in shared code.
 */
enum class MaConnectionMode {
    LOCAL,
    REMOTE,
    PROXY
}
